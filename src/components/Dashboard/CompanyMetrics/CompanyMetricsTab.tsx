
import React from "react";
import { CompanyMetrics } from "./CompanyMetrics";

export function CompanyMetricsTab({ clients }: any) {
  // Could add additional client filtering or metrics here
  return <CompanyMetrics />;
}
